{
  "name": "eventique-root",
  "private": true,
  "version": "1.0.0",
  "description": "Root workspace for Eventique (frontend in my-app, backend in api)",
  "scripts": {
    "api": "node api/server.js",
    "api:dev": "nodemon api/server.js",
    "frontend": "npm start --prefix my-app",
    "install:all": "npm install --prefix api && npm install --prefix my-app"
  },
  "dependencies": {
    "@mui/x-charts": "^8.22.0",
    "@react-three/drei": "^10.7.7",
    "@react-three/fiber": "^9.4.0",
    "three": "^0.181.2"
  }
}
